class OrderModal {
  final String pname, pimage, quantity, price, method, uid;

  OrderModal(
      {required this.uid,
        required this.pname,
        required this.pimage,
        required this.quantity,
        required this.price,
        required this.method});
}